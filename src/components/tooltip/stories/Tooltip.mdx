import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Title, SectionTitle } from '../../../../.storybook/components';
import { Tooltip } from '../Tooltip';

<Title
  title="Tooltip"
  subtitle="Used to reveal extra guidance or information to the user."
/>

<SectionTitle>Simple</SectionTitle>

Change the placement of the tooltip with respect to the anchor position by modifying the `placement` prop.

<Preview>
  <Story id="components-tooltip--simple"/>
</Preview>

<SectionTitle>Tooltip Props</SectionTitle>
<Props of={ Tooltip } />
